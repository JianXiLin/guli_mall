server:
  port: 8000
spring:
  datasource:
    #MySQL配置
    driverClassName: com.mysql.jdbc.Driver
    username: root
    password: 123456
    url: jdbc:mysql://localhost:3300/gulimall-ums?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serve
  application:
    name: gulimall_member
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
mybatis-plus:
  mapper-locations: "classpath:/mapper/**/*.xml"
  global-config:
    db-config:
      id-type: auto